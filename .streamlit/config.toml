# ============================================================
# DataPipeline Pro — Tema do Streamlit
# Alinhado à identidade visual do site (console de dados).
# ============================================================

[theme]
base = "dark"
primaryColor = "#22D3EE"
backgroundColor = "#0b1120"
secondaryBackgroundColor = "#101829"
textColor = "#e2e8f0"
font = "sans serif"

[server]
headless = true

[browser]
gatherUsageStats = false
